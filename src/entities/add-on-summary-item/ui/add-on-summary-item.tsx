import * as React from 'react';
import {AddOnType, PriceType} from "shared/@types";
import styled from "styled-components";

const StyledContainer = styled.div`
`;

type Props = {
    addOn: AddOnType,
    price: PriceType,
};

export const AddOnSummaryItem = (props: Props) => {
    return (
        <div>

        </div>
    );
};
